export func main() int {
    return name() + lenof("Duda") + lenof2("!!")
}

func name() int {
    return "Hallo".len()
}

func lenof(str string) int {
    return str.len()
}

func lenof2(str string) int {
    var s2 = str
    return s2.len()
}