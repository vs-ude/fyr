type List struct {
    next *List
    value int
}

export func main() int {
    var list *List = {value: 42}
    var arr [2]List = [{}, {}]
    var tuple (List, List) = ({}, {})
    var arr2 [2]List = [{value:23, next: {}}, {}]
//    var ref ~List = list
    var slice []List = [{}, {}]
    arr[1].value = getValue(list)
    var arr3 [2]*List = [{value: 3}, {value: 5}]
    var counter = 0
    return list.value + slice.len()
}

func getValue(list &List) int {
    return list.value
}