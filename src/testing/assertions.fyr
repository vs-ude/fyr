export func Test.assertEqual<T>(actual T, expected T) {
    if (actual != expected) {
        this.results[current][step] = false
    }
    this.results[current][step] = true
    this.nextStep()
}

// export func Test.assertEqual(actual bool, expected bool) {
//     this.assertEqual(actual, expected)
// }
