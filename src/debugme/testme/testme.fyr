import "debugme/foo/bar"
import {square, Dummy} from "debugme/foo/bar"

export func main() int {
    var l bar.LinkedList<int> = {}
    var d Dummy = {}
    return bar.square(4) + bar.min(1,3) + l.value + d.value + square(3)
}