func search(arr []int, val int) int {
    for(var i, v in arr) {
        if (v == val) {
            return i
        }
    }
    return 0
}

func fudel() {
    var arr []byte = <[]byte>"Hallo"
    var str string = <string>arr
    var b bool = <bool>1
}

export func main() int {
    var str = "Hudel"
    var arr = [4]int[123, 234, 345, 456]
    return search(arr[:], 234) + str.len()
}
