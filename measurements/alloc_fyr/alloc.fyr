import . {
    func logString(string)
    func logNumber(uint)
} from "imports"

type Buffer struct {
    len int
    data [4096]byte
}

export func main() int {
    var b *Buffer
    var l int
    for(var i = 0; i < 100000; i++) {
        b = &{}
        l += b.len
    }
    return l
}