type Point struct {
    a int
    b int
}

export func main() int {
    var point = Point{a:1, b:2}
    var p = &point
    var r = 0;
    for(var i = 0; i < 320000000; i++) {
        r += p.a + p.b
    }
    return r
}

export func main2() int {
    var point = Point{a:1, b:2}
    var p #Point = &point
    var r = 0;
    for(var i = 0; i < 320000000; i++) {
        r += p.a + p.b
    }
    return r
}
